meta {
  name: Create Device
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/sensors
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Sensor SD002",
    "ip": "127.0.1.2",
    "location": "Some other random city",
    "protocol": "HTTP",
    "model": "S55-HTTP"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

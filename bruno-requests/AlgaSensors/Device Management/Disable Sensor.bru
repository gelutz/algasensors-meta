meta {
  name: Disable Sensor
  type: http
  seq: 4
}

delete {
  url: {{url}}{{managerPort}}/api/sensors/:sensorId/enable
  body: none
  auth: inherit
}

params:path {
  sensorId: 0NDSGCD30RD6T
}

settings {
  encodeUrl: true
  timeout: 0
}
